{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "yarn install && ts-node index.ts",
    "heroku-postbuild": "cd frontend && yarn install && yarn run build",
    "start-locally": "(cd frontend && yarn build) && (cd ../noissue-test && yarn start)"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "14.16.0",
    "npm": "7.6.2",
    "yarn": "1.22.5"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "@types/node": "^14.14.37",
    "@types/cors": "^2.8.10",
    "@types/express": "^4.17.11",
    "ts-node": "^9.1.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "typescript": "^4.2.3"
  }
}
